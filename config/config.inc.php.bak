<?php

# Database management system to use
$DBMS = 'MySQL'; // Don't change unless you know what you're doing

# Database variables
$_DVWA = array();
$_DVWA[ 'db_server' ]   = '127.0.0.1';
$_DVWA[ 'db_database' ] = 'dvwa';
$_DVWA[ 'db_user' ]     = 'root';
$_DVWA[ 'db_password' ] = '';  // Replace with your actual MySQL user password if different
$_DVWA[ 'db_port']      = '3306';

# ReCAPTCHA settings (leave blank if not using)
$_DVWA[ 'recaptcha_public_key' ]  = '';
$_DVWA[ 'recaptcha_private_key' ] = '';

# Default security level
$_DVWA[ 'default_security_level' ] = 'impossible';  // can be 'low', 'medium', 'high', or 'impossible'

# Default locale
$_DVWA[ 'default_locale' ] = 'en';

# Disable authentication (set to true only for testing tools)
$_DVWA[ 'disable_authentication' ] = false;

define ('MYSQL', 'mysql');
define ('SQLITE', 'sqlite');

# SQLi DB Backend (for SQLi/Blind SQLi labs only)
$_DVWA['SQLI_DB'] = MYSQL;
// $_DVWA['SQLITE_DB'] = 'sqli.db';

?>
